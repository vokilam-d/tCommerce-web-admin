import { NormalizedPhonePipe } from './normalized-phone.pipe';

describe('NormalizedPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizedPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
