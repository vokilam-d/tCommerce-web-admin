import { DraggableListDirective } from './draggable-list.directive';

describe('DraggableListDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableListDirective();
    expect(directive).toBeTruthy();
  });
});
