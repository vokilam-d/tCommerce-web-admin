import { DraggableItemDirective } from './draggable-item.directive';

describe('DraggableItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableItemDirective();
    expect(directive).toBeTruthy();
  });
});
