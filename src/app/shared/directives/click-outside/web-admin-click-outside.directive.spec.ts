import { WebAdminClickOutsideDirective } from './web-admin-click-outside.directive';

describe('WebAdminClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new WebAdminClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
